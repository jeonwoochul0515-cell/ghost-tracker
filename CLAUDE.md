# CLAUDE.md — Ghost Bidder Tracker (프로젝트 로컬)

> 글로벌 `~/.claude/CLAUDE.md` (행동 가이드라인)와 병합되어 적용됩니다.
> 이 파일은 **이 프로젝트의 도메인·법적·디자인 컨텍스트**만 다룹니다.

---

## 프로젝트 정체성

**Ghost Bidder Tracker** — 부산 학교급식 유령입찰(분신 응찰) 추적기.
공익 모니터링 도구로, 법무법인 청송과 협력하여 운영합니다.

## 배경

- 학교급식 식자재 입찰은 **제한최저가 + 추첨** 방식
- 한 운영자가 가족·지인 명의로 사업자 N개를 만들어 같은 추첨에 분산 응찰
- → 추첨 당첨확률을 N배로 부풀리는 **"분신술"** 이 시장 왜곡의 핵심 메커니즘
- eaT 시스템은 동일 대표자·동일 IP는 차단하지만, 사업자 등록정보(주소·가족관계)
  동일성은 검증하지 않음 → 이 사각지대를 데이터로 추적

## 목표

공공데이터(eaT API + 국세청 API + 도로명주소) + 공개 판례를 결합해
"같은 실체로 추정되는 사업자 클러스터"를 자동 탐지하고
부산 시민·기자·연구자에게 시각화해 공개하는 공익 도구.

## 기술 스택

- **프론트**: React 18 + Vite + TypeScript
- **스타일**: TailwindCSS + shadcn/ui 스타일 (직접 작성)
- **백엔드**: Supabase (Postgres + Edge Functions + Auth)
- **차트**: Recharts
- **지도**: react-leaflet (부산 600개 학교 시각화)
- **상태**: Zustand
- **라우팅**: React Router v6
- **폼**: React Hook Form + Zod
- **테스트**: Vitest + React Testing Library

## 법적 가이드라인 (반드시 준수)

| 항목 | 금지 | 권장 |
|---|---|---|
| 단정 표현 | "유령사업자다", "페이퍼컴퍼니" | "의심 클러스터", "통계적 패턴", "추정" |
| 사업자번호 | 풀 번호 노출 | 중간 마스킹 `123-XX-67890` |
| 대표자명 | 풀네임 노출 | 성씨만 `김OO` |
| 주소 | 번지·호수까지 노출 | 동까지만 |
| 출처 표기 | 누락 | 공공데이터포털·공공누리 명시 |
| 이의제기 | 무대응 | **24시간 내 비공개 처리** 가능한 구조 |

모든 화면에 면책 표시 (`<Disclaimer />`)와 출처를 노출합니다.

## 디자인 톤

탐사보도 매체(ProPublica · OCCRP) 톤의 다크 편집 미니멀.

- **폰트**: Bodoni Moda (영문 display) + Noto Serif KR (본문) + IBM Plex Mono (메타)
- **주조색**: `#0a0a0a` 배경 / `#f5f1e8` 본문 / `#d4ad3c` 강조 (aged gold)
- **위험도색**: high `#c14545` / mid `#d4843c` / low `#5a8a5a`
- 필름 그레인 노이즈 오버레이로 인쇄 매체 질감

전체 컬러 토큰은 `tailwind.config.ts` 의 `theme.colors` 참조.

## 개발 진행 방식

`ghost-tracker-prompts.md` 의 **P00 ~ P18** 순서대로 점진적으로 구현합니다.

- 각 단계가 끝나면 검증(`npm run dev`, `npm run test`, `npx tsc --noEmit`) 후 커밋
- **임의 확장 금지** — 다음 프롬프트가 들어올 때만 그 단계를 처리
- 진행 상태: `checklist.md` (체크박스), 결정 근거: `context-notes.md`

## 운영 메모

- 운영주체: 법무법인 청송 협력 / 부산 시민 공익 모니터링
- 자문 변호사: 김창희 (법적 검토)
- 도메인 검토: 우철님 (협동조합 경험)
- 위치: 부산 연제구
